[project]
name = "apb-studio"
version = "0.0.0"
description = "Corpus dashboard + Snakemake pipeline driving the anndata_proteomics (APB) conversion CLI."
requires-python = ">=3.13"
dependencies = [
    "anndata-proteomics",
    "snakemake>=8",
    "dash>=3",
    "dash-ag-grid",
    "pandas",
    "platformdirs>=4",
    "pydantic>=2",
    "pyyaml",
    "mudata",
    "anndata",
]

[tool.uv.sources]
anndata-proteomics = { path = "../apb", editable = true }

[project.optional-dependencies]
dev = ["pytest", "ruff"]

[project.scripts]
apb-studio = "apb_studio.dashboard:main"
apb-studio-testdata = "apb_studio.testdata_app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/apb_studio"]
